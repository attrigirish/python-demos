Python Module

Simply, a module is a file consisting of Python code. A module can define functions, classes and variables. A module can also include runnable code.


Import module(s) using import keyword:

import <module-name>

e.g.

import math